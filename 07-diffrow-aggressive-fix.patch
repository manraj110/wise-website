--- a/src/app/globals.css	2026-06-30 06:42:03.532654000 +0000
+++ b/src/app/globals.css	2026-06-30 06:46:27.342469942 +0000
@@ -667,47 +667,30 @@
      losing it on mobile is an acceptable trade for not cutting off
      the words it's pointing at.
      ------------------------------------------------------------------ */
-  .diff-row {
-    align-items: flex-start !important;
-    /* The heading child already had min-width:0/flex:1 so IT could
-       shrink — but .diff-row itself sits inside a flex column wrapper
-       and, as a flex item, defaults to min-width:auto, which floors
-       it at its own content's natural width. With 4 long phrases like
-       "Institution-Focused Approach" that natural width is wider than
-       the phone screen, so the row (and the heading inside it) never
-       actually got the chance to wrap — the row itself was the thing
-       overflowing, not just the text. */
+  /* MAXIMALLY AGGRESSIVE FIX — every previous attempt assumed a specific
+     ancestor depth or relied on :has(), and kept missing. This drops
+     all of that: every element from #about down to .diff-row gets
+     min-width:0 directly (safe to apply broadly — it only removes the
+     auto-floor, doesn't force any sizing), and the grid item containing
+     the diff-row list specifically gets width:100% (scoped narrowly via
+     :has() so it doesn't touch the unrelated left-hand text column). */
+  #about,
+  #about *,
+  #about .diff-row,
+  #about .diff-row * {
     min-width: 0 !important;
-    width: 100% !important;
-    max-width: 100% !important;
   }
-  /* The wrapper around all 4 diff-row items has no dedicated class —
-     it's targeted here via :has() since it only carries Tailwind
-     utility classes (gap-16 = 4rem, sized for desktop's tall zigzag
-     staircase). Four single-line rows don't need that much breathing
-     room once they're just stacked plainly on mobile. */
-  /* THE ACTUAL ROOT CAUSE: the immediate grid item wrapping the whole
-     diff-row list ("relative flex justify-center md:min-h-[620px]") is
-     itself a CSS Grid item (child of "grid lg:grid-cols-2"). Grid items
-     default to min-width:auto just like flex items — so THIS div was
-     the one flooring at its content's widest row and refusing to
-     shrink, before any fix on .diff-row or its flex wrappers below it
-     ever got a chance to matter. Fixing it here, at the true top of
-     the chain, is what actually lets every row underneath wrap. */
   #about .grid {
     grid-template-columns: 1fr !important;
   }
-  #about .grid > div {
-    min-width: 0 !important;
-    width: 100% !important;
+  #about .grid > div:has(.diff-row),
+  #about .grid > div:has(.diff-row) > div {
     max-width: 100% !important;
-  }
-
-  div:has(> .diff-row) {
-    gap: 1.5rem !important;
-    min-width: 0 !important;
     width: 100% !important;
   }
+  .diff-row {
+    align-items: flex-start !important;
+  }
   .diff-line {
     width: 28px !important;
     margin-top: 0.65rem !important;
@@ -716,6 +699,13 @@
     font-size: 1.35rem !important;
     min-width: 0 !important;
     flex: 1 1 auto !important;
+    /* Safari-specific safety net: even with correct flex-shrink/
+       min-width, long unbroken phrases can still refuse to wrap in
+       WebKit's flex line-breaking in rare cases. Forcing wrap at the
+       CSS level (not just relying on flex shrink) guarantees the text
+       itself can never push its row wider than the viewport. */
+    overflow-wrap: break-word !important;
+    word-break: break-word !important;
   }
 
   /* ------------------------------------------------------------------
