--- a/src/app/globals.css	2026-06-30 17:51:13.585364000 +0000
+++ b/src/app/globals.css	2026-06-30 18:50:22.095713763 +0000
@@ -728,4 +728,32 @@
     width: 100% !important;
     padding: 24px 20px !important;
   }
+
+  /* ------------------------------------------------------------------
+     SERVICES PAGE — "New School Development" timeline forcing the
+     whole card wider than the screen on mobile (Vision/Planning/
+     Systems/Launch pills never wrap, and the column has no
+     min-width:0, so the grid track + card stretch to fit them).
+     ------------------------------------------------------------------ */
+  .new-school-left-panel {
+    min-width: 0 !important;
+  }
+  .new-school-timeline {
+    flex-wrap: wrap !important;
+    row-gap: 0.6rem !important;
+  }
+
+  /* ------------------------------------------------------------------
+     SERVICES PAGE — "Every School is Different" profile tabs.
+     gap-x-12 (48px) is sized for a single desktop row; on mobile the
+     three tabs wrap one-per-line but keep that 48px gap, leaving big
+     dead gaps between them. Stack them deliberately with a tighter,
+     even vertical rhythm instead.
+     ------------------------------------------------------------------ */
+  .profile-tabs-row {
+    flex-direction: column !important;
+    align-items: flex-start !important;
+    column-gap: 0 !important;
+    row-gap: 1.1rem !important;
+  }
 }
